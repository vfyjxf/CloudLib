package dev.vfyjxf.cloudlib.utils;

/**
 * Search internal reference.
 * For JustEnoughCharacters to replace.
 */
public final class SearchReference {

    public static boolean contains(String string, CharSequence s2) {
        return string.contains(s2);
    }

    private SearchReference() {
    }
}
