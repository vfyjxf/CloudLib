package dev.vfyjxf.cloudlib.api.ui.style;

/**
 * Represents a physical edge of a rectangle.
 * <p>
 * Used by layout properties that target taffy {@code Rect<T>} values
 * (margin/padding/border/inset).
 */
public enum Edge {
    TOP,
    RIGHT,
    BOTTOM,
    LEFT;
}
