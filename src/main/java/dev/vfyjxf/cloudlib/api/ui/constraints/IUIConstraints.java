package dev.vfyjxf.cloudlib.api.ui.constraints;

/**
 * Because it is not possible to take into account all possible UI constraints,
 * <p>
 * the type does not provide any built-in methods for setting constraints,
 * <p>
 * so we use the extension functions provided by manifold to solve this problem.
 */
public interface IUIConstraints {
}
