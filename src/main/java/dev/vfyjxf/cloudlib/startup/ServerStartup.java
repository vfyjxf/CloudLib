package dev.vfyjxf.cloudlib.startup;

import net.neoforged.bus.api.IEventBus;

public class ServerStartup extends CommonStartup {
    public ServerStartup(IEventBus modBus) {
        super(modBus);
    }
}
